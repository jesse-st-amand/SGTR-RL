backend: local

artifacts:
  root_dir: results

local:
  device: auto
  dtype: auto
  max_seq_length: 4096
  eval_batch_size: 8
  gradient_checkpointing: true
  load_in_4bit: false
  attention_implementation: sdpa
  cache_dir: null
  lora_dropout: 0.0
  target_modules: all-linear
